This repo contains ansible code to install jenkins job-builder

When cloning from github, simply run:

    rake

When using galaxy, simply run:

    ansible-galaxy install Azulinho.azulinho-jenkins-job-builder
